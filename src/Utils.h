#ifndef UTILS_H
#define UTILS_H

#include "LoanCalculator.h"

/**
 * @brief Load default config values from config.txt into calculator
 * @param calc LoanCalculator object to load values into
 */
void loadConfig(LoanCalculator &calc);

#endif
